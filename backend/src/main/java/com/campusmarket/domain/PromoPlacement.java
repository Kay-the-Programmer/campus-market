package com.campusmarket.domain;

/** Where on the home page a {@link PromoSlot} renders. */
public enum PromoPlacement {
    /** The full-width hero carousel at the top. */
    CAROUSEL,
    /** The "Special offers" tile grid below it. */
    BENTO
}
