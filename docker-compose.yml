# Base stack: safe by default.
#
# Nothing here publishes a port to the host and nothing here enables a dev
# convenience. Those live in docker-compose.override.yml, which Compose loads
# automatically for local work and which the production command never includes:
#
#   local  ->  docker compose up -d --build
#   VPS    ->  docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
#
# That asymmetry is the point. A single file serving both environments is what
# let dev-only settings ship, and no amount of care in one file fixes it.

services:
  postgres:
    image: postgres:16-alpine
    container_name: campusmarket-db
    environment:
      POSTGRES_DB: campusmarket
      POSTGRES_USER: campusmarket
      # No default. The :? form makes Compose refuse to start when this is
      # unset rather than quietly falling back to a guessable password - the
      # behaviour you want for the credential guarding every user record.
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
    volumes:
      - campusmarket-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U campusmarket -d campusmarket"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    # Container logs are the only record of what happened; uncapped they are
    # also a way to fill the disk and take the host down with them.
    logging:
      driver: json-file
      options: {max-size: "10m", max-file: "3"}

  api:
    build:
      context: ./backend
    container_name: campusmarket-api
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/campusmarket
      SPRING_DATASOURCE_USERNAME: campusmarket
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      CAMPUSMARKET_UPLOADS_DIR: /app/uploads
      CAMPUSMARKET_CORS_ORIGINS: "${CAMPUSMARKET_CORS_ORIGINS:-https://salepilot.space,https://www.salepilot.space,https://campusmarket.salepilot.space,https://campus-market-mu.vercel.app,https://www.campus-market-mu.vercel.app,http://localhost:3000,http://localhost:5173}"
      CAMPUSMARKET_SEED_DEMO_DATA: "${CAMPUSMARKET_SEED_DEMO_DATA:-true}"
      CAMPUSMARKET_ADMIN_EMAIL: "${CAMPUSMARKET_ADMIN_EMAIL:-}"
      CAMPUSMARKET_ADMIN_PASSWORD: "${CAMPUSMARKET_ADMIN_PASSWORD:-}"
      # Optional: paste a Firebase service-account key's JSON contents (as one
      # line) into a .env file at the repo root as CAMPUSMARKET_FIREBASE_CREDENTIALS_JSON
      # to enable "Continue with Google" and push notifications. Left unset, both
      # are simply disabled - notifications still appear in-app, they just do not
      # reach devices, and everything else works the same.
      CAMPUSMARKET_FIREBASE_CREDENTIALS_JSON: "${CAMPUSMARKET_FIREBASE_CREDENTIALS_JSON:-}"
      # Optional: SMTP, for notification email and admin campaigns. Left unset,
      # Spring builds no JavaMailSender, SmtpMailer reports itself disabled, and
      # every message is written to the container log instead - which is what a
      # local run wants. Campaigns refuse to send at all rather than pretending.
      #
      # Any provider that speaks SMTP works; nothing here names one. Port 587
      # with STARTTLS is the default in application.yml.
      SPRING_MAIL_HOST: "${SPRING_MAIL_HOST:-}"
      SPRING_MAIL_PORT: "${SPRING_MAIL_PORT:-587}"
      SPRING_MAIL_USERNAME: "${SPRING_MAIL_USERNAME:-}"
      SPRING_MAIL_PASSWORD: "${SPRING_MAIL_PASSWORD:-}"
      # The envelope sender. Must be on a domain the provider has verified, or
      # mail is rejected or junked in a way that looks like it never sent.
      CAMPUSMARKET_MAIL_FROM: "${CAMPUSMARKET_MAIL_FROM:-}"
      CAMPUSMARKET_MAIL_FROM_NAME: "${CAMPUSMARKET_MAIL_FROM_NAME:-CampusMarket}"
      # Public URL of the frontend. Becomes the unsubscribe link and every deep
      # link in an email, so a relative or localhost value produces mail whose
      # links do not work for the recipient.
      CAMPUSMARKET_APP_BASE_URL: "${CAMPUSMARKET_APP_BASE_URL:-}"
      CAMPUSMARKET_MAIL_BATCH_SIZE: "${CAMPUSMARKET_MAIL_BATCH_SIZE:-25}"
      CAMPUSMARKET_MAIL_BATCH_PAUSE_MILLIS: "${CAMPUSMARKET_MAIL_BATCH_PAUSE_MILLIS:-1000}"
      # Optional: move listing/promo image uploads to a Google Cloud Storage
      # bucket instead of the local volume below. Set both:
      #   CAMPUSMARKET_GCS_BUCKET - the bucket name (not a full URL)
      #   CAMPUSMARKET_GCS_CREDENTIALS_JSON - a service-account key's JSON
      #     contents (as one line); can be the same key used for Firebase
      #     above if that service account also has Storage Object Admin on
      #     this bucket
      # The bucket needs public read so served URLs work without a signed
      # request: Cloud Console -> bucket -> Permissions -> Grant Access ->
      # principal "allUsers", role "Storage Object Viewer" (or equivalently
      # `gcloud storage buckets add-iam-policy-binding gs://BUCKET
      # --member=allUsers --role=roles/storage.objectViewer`). Left unset,
      # uploads are written to local disk instead - everything else works
      # the same.
      CAMPUSMARKET_GCS_BUCKET: "${CAMPUSMARKET_GCS_BUCKET:-}"
      CAMPUSMARKET_GCS_CREDENTIALS_JSON: "${CAMPUSMARKET_GCS_CREDENTIALS_JSON:-}"
      # Preferred over pasting the key JSON above: point at a mounted key file
      # instead. GcsConfig leaves credentials to the client library when
      # gcs-credentials-json is blank, and the library reads this path. Keeping
      # a private key in a gitignored file beats inlining it in an env var,
      # where it lands in shell history and `docker inspect` output.
      GOOGLE_APPLICATION_CREDENTIALS: "${GOOGLE_APPLICATION_CREDENTIALS:-}"
    volumes:
      - campusmarket-uploads:/app/uploads
      # The service-account key, when one is in use. `:ro` because the API only
      # ever reads it. Harmless when the file is absent and GCS is unconfigured
      # - Compose creates an empty placeholder and nothing reads it.
      - ./backend/gcs-key.json:/app/gcs-key.json:ro
    healthcheck:
      # Actuator exposes health only (see application.yml). wget rather than
      # curl because the JRE alpine image ships busybox, not curl.
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep -q UP"]
      interval: 15s
      timeout: 5s
      retries: 5
      # Generous: a cold JVM plus Flyway migrations against an empty database
      # is comfortably slower than the interval above, and a start_period that
      # is too tight restarts the container mid-migration.
      start_period: 90s
    restart: unless-stopped
    logging:
      driver: json-file
      options: {max-size: "10m", max-file: "3"}

volumes:
  campusmarket-pgdata:
  campusmarket-uploads:
